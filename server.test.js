import { describe, expect } from '@jest/globals';

describe('Pruebas de configuración inicial', () => {
    test('debería pasar esta prueba inicial sin problemas', () => {
        expect(1).toBe(1);
    });
});